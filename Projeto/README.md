
Projeto preparado - minimal backend + simple frontend.

Steps:
1. Copy .env.example to .env and set MONGO_URI and JWT_SECRET
2. npm install
3. npm start
4. Open http://localhost:5000 to see API and serve public/ via static server if desired.

Note: This package excludes node_modules for size. Install dependencies with npm.
